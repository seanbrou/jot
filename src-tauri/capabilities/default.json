{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main and capture windows",
  "windows": ["main", "capture"],
  "permissions": [
    "core:default",
    "opener:default",
    "global-shortcut:default",
    "positioner:default",
    "clipboard-manager:default",
    "sql:default"
  ]
}
